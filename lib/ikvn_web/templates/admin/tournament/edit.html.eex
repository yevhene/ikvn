<h2><%= gettext "Edit Tournament \"%{tournament}\"", tournament: @tournament.name %></h2>

<%= render "_form.html", Map.put(assigns, :action, Routes.admin_tournament_path(@conn, :update, @tournament)) %>

<hr />
<span><%= link gettext("Back"), to: Routes.admin_tournament_path(@conn, :show, @tournament) %></span>
